<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RadaeeWinUI.Controls.SearchToolbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Padding="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            
            <TextBox x:Name="SearchTextBox" 
                     PlaceholderText="Enter search text..." 
                     Width="250" 
                     VerticalAlignment="Center"
                     TextChanged="SearchTextBox_TextChanged"
                     KeyDown="SearchTextBox_KeyDown"/>
            
            <Button x:Name="SearchButton" 
                    Content="Search" 
                    Click="SearchButton_Click"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Start search"/>
            
            <Button x:Name="PrevButton" 
                    Content="Previous" 
                    Click="PrevButton_Click"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Go to previous result"/>
            
            <Button x:Name="NextButton" 
                    Content="Next" 
                    Click="NextButton_Click"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Go to next result"/>
            
            <Button x:Name="ResetButton" 
                    Content="Reset" 
                    Click="ResetButton_Click"
                    IsEnabled="False"
                    ToolTipService.ToolTip="Clear search"/>
            
            <Border Width="1" Background="Gray" Margin="4,0"/>
            
            <CheckBox x:Name="WholeWordCheckBox" 
                      Content="Whole Word" 
                      VerticalAlignment="Center"
                      ToolTipService.ToolTip="Match whole words only"/>
            
            <CheckBox x:Name="MatchCaseCheckBox" 
                      Content="Match Case" 
                      VerticalAlignment="Center"
                      ToolTipService.ToolTip="Case sensitive search"/>
            
            <Border Width="1" Background="Gray" Margin="4,0"/>
            
            <TextBlock x:Name="ResultsTextBlock" 
                       Text="" 
                       VerticalAlignment="Center" 
                       Margin="8,0,0,0"
                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
        </StackPanel>

        <Button Grid.Column="1" 
                Content="✕" 
                Click="CloseButton_Click"
                ToolTipService.ToolTip="Close search toolbar"
                VerticalAlignment="Center"
                MinWidth="32"
                Padding="8,4"/>
    </Grid>
</UserControl>
