<?xml version="1.0" encoding="utf-8"?>
<local:PDFView
    x:Class="RadaeeWinUI.Controls.PDFView.DualPageScrollView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RadaeeWinUI.Controls.PDFView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer x:Name="MainScrollViewer"
                  HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto"
                  ZoomMode="Disabled"
                  ViewChanged="MainScrollViewer_ViewChanged">
        <Grid>
            <Canvas x:Name="PageCanvas" 
                    Background="Transparent"
                    PointerPressed="PageCanvas_PointerPressed"
                    PointerMoved="PageCanvas_PointerMoved"
                    PointerReleased="PageCanvas_PointerReleased">
            </Canvas>
            <Canvas x:Name="AnnotationCanvas"
                    Background="Transparent"
                    IsHitTestVisible="False">
            </Canvas>
        </Grid>
    </ScrollViewer>
</local:PDFView>
