<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="RadaeeWinUI.Controls.AttachmentListDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:RadaeeWinUI.Models"
    Title="PDF Attachments"
    PrimaryButtonText="Close"
    DefaultButton="Primary">

    <Grid MinWidth="500" MinHeight="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Document Attachments" 
                   FontSize="16" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <InfoBar x:Name="StatusInfoBar"
                 Grid.Row="1"
                 IsOpen="False"
                 Margin="0,0,0,10"/>

        <ListView Grid.Row="2" 
                  x:Name="AttachmentListView"
                  SelectionMode="Single"
                  BorderBrush="{ThemeResource SystemControlForegroundBaseMediumLowBrush}"
                  BorderThickness="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:AttachmentData">
                    <Grid Padding="10" ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Text="{x:Bind Name}" 
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"/>
                            <TextBlock Text="{x:Bind Description}" 
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                       TextWrapping="Wrap"
                                       FontSize="12"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" 
                                    Orientation="Horizontal" 
                                    Spacing="8"
                                    VerticalAlignment="Center">
                            <Button Content="Download" 
                                    Click="Download_Click"
                                    Tag="{x:Bind Index}"/>
                            <Button Content="Delete" 
                                    Click="Delete_Click"
                                    Tag="{x:Bind Index}"
                                    Style="{StaticResource AccentButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
