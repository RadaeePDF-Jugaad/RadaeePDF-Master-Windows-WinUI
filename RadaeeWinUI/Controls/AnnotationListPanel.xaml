<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="RadaeeWinUI.Controls.AnnotationListPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RadaeeWinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <TextBlock Text="Annotations" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBox x:Name="SearchBox" PlaceholderText="Search annotations..." TextChanged="SearchBox_TextChanged"/>
        </StackPanel>

        <ListView x:Name="AnnotationListView" 
                  Grid.Row="1" 
                  SelectionMode="Single"
                  SelectionChanged="AnnotationListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" Width="32" Height="32" CornerRadius="4" Background="{ThemeResource AccentFillColorDefaultBrush}" Margin="0,0,12,0">
                            <TextBlock Text="{Binding TypeIcon}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                        </Border>
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding TypeName}" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="{Binding PageInfo}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{Binding Content}" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextTrimming="CharacterEllipsis" MaxLines="1" Margin="0,4,0,0" Visibility="{Binding HasContent}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="1" 
                   Text="No annotations" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Visibility="{Binding ElementName=AnnotationListView, Path=Items.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
    </Grid>
</UserControl>
